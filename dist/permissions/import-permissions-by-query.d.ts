import { IDirectus, TypeMap } from '@directus/sdk';
export declare const importPermissionsByQuery: (directus: IDirectus<TypeMap>, query: any, sourceFile: string, roleId: string | null) => Promise<void>;
